import "../styles/App.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1 className="navbar-heading">User Management Dashboard</h1>
    </nav>
  );
};

export default Navbar;
